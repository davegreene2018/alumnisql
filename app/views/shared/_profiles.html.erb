

  <div class="forums">
    
    <% @profiles.each do |profile| %>
      
          <div class="media-content">
          <div class="content">
            <%= truncate(profile.status, length: 140) %>
            <p><em><small>Posted <%= time_ago_in_words(profile.created_at ) %> ago in
              
              
              </small>
              </em>
            </p>
            
          </div>
        </div>

        <%# if forum_author(forum) || has_role?(:admin) %>

        <div class="column has-text-left">
          
          <%= link_to 'Edit', edit_profile_path(profile), class:'button' %>
          <%= link_to 'Delete', profile, method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %>
        </div>
        <%# end %>

      
    <% end %>
  </div>
